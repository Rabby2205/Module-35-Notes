/*
1.local scope or local variable is which variable is decleare inside of functio 
{you can access only inside mother function not outside of this function} .
2.global scope or global variable is which variable decleare outside of function 
{you can access every-where where you want }. 
3.block-scope is not useable to outside of your braket area 
block-scope only for cosnt and let
but var is not block-scope variable declearation(var Hoisting declearation). 
4.var is involb wtih global leaking(hoisting) .
5.const and let is Block Scope(not hoisting )
*/